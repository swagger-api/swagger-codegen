swagger: "2.0"
info:
  title: "Test"
  version: "2"
paths:
  /test:
    get:
      produces:
        - "application/json"
      responses:
        200:
          description: "success"
          schema:
            $ref: "#/definitions/Test"
definitions:
  CustomDateTime:
    type: "string"
    format: "date-time"
  Test:
    type: "object"
    properties:
      date:
        type: "string"
        format: "date-time"
      dateRef:
        $ref: '#/definitions/CustomDateTime'
      dateArr:
        type: array
        items:
          type: "string"
          format: "date-time"
      dateRefArr:
        type: array
        items:
          $ref: '#/definitions/CustomDateTime'
      dateMap:
        type: object
        additionalProperties:
          type: "string"
          format: "date-time"
      dateRefMap:
        type: object
        additionalProperties:
          $ref: '#/definitions/CustomDateTime'
