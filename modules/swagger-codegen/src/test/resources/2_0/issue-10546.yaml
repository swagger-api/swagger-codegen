swagger: '2.0'
info:
  description: Demo
  version: 1.0.0
  title: Demo for Boolean-element Bug
schemes:
  - https
consumes:
  - application/json
produces:
  - application/json
paths:
  /types:
    get:
      produces:
        - application/json
      responses:
        200:
          description: OK
definitions:
  Boolean:
    type: boolean
    description: True or False indicator
    enum:
      - true
      - false
  Interos:
    type: integer
    format: int32
    description: True or False indicator
    enum:
      - 0
      - 1
      - 2
      - 3
      - 4
      - 5
      - 6
  Numeros:
    type: number
    description: some number
    enum:
      - 7
      - 8
      - 9
      - 10
