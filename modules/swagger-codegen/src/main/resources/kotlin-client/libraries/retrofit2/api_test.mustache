package {{package}}

import {{packageName}}.infrastructure.ApiClient
{{#imports}}import {{import}}
{{/imports}}
import org.junit.Assert
import org.junit.Before
import org.junit.Test

/**
 * API tests for {{classname}}
 */
class {{classname}}Test {

    private lateinit var api: {{classname}}

    @Before
    fun setup() {
        api = ApiClient().createService({{classname}}::class.java)
    }
    {{#operations}}
    {{#operation}}

    /**
     * {{summary}}
     *
     * {{notes}}
     */
    @Test
    fun {{operationId}}Test() {
        {{#allParams}}
        val {{paramName}}: {{{dataType}}}? = null
        {{/allParams}}
        // val response = api.{{operationId}}({{#allParams}}{{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}}).execute()
        // Assert.assertEquals(200, response.code())
        // TODO: test validations
    }
    {{/operation}}
    {{/operations}}
}
