/* 
 * Swagger Petstore
 *
 * This is a sample server Petstore server.  You can find out more about Swagger at [http://swagger.io](http://swagger.io) or on [irc.freenode.net, #swagger](http://swagger.io/irc/).  For this sample, you can use the api key `special-key` to test the authorization filters.
 *
 * OpenAPI spec version: 1.0.0
 * Contact: apiteam@swagger.io
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use hyper;
use hyper::header::{HeaderName, HeaderValue};
use hyper::rt::{Future, Stream};
use serde_json;

use super::{Error, configuration};

pub struct StoreApiClient<'a, C: hyper::client::connect::Connect> {
    configuration: &'a configuration::Configuration<C>,
}

impl<'a, C: hyper::client::connect::Connect> StoreApiClient<'a, C> {
    pub fn new(configuration: &'a configuration::Configuration<C>) -> StoreApiClient<'a, C> {
        StoreApiClient {
            configuration: configuration,
        }
    }
}

pub trait StoreApi {
    fn delete_order(&self, order_id: &str) -> Box<dyn Future<Item = (), Error = Error<serde_json::Value>>>;
    fn get_inventory(&self, ) -> Box<dyn Future<Item = ::std::collections::HashMap<String, i32>, Error = Error<serde_json::Value>>>;
    fn get_order_by_id(&self, order_id: i64) -> Box<dyn Future<Item = ::models::Order, Error = Error<serde_json::Value>>>;
    fn place_order(&self, body: ::models::Order) -> Box<dyn Future<Item = ::models::Order, Error = Error<serde_json::Value>>>;
}

impl<'a, C: hyper::client::connect::Connect>StoreApi for StoreApiClient<'a, C> where C: 'static {
    fn delete_order(&self, order_id: &str) -> Box<dyn Future<Item = (), Error = Error<serde_json::Value>>> {
        let method = hyper::Method::DELETE;


        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/store/order/{orderId}?{}", self.configuration.base_path, query_string, orderId=order_id);

        let mut req = hyper::Request::new(hyper::Body::empty());
        *req.method_mut() = method.clone();

        let uri: hyper::Uri = uri_str.parse().unwrap();
        *req.uri_mut() = uri.clone();

        if let Some(ref user_agent) = self.configuration.user_agent {
            req.headers_mut().insert(hyper::header::CONTENT_TYPE, HeaderValue::from_str(&user_agent).unwrap());
        }




        // send request
        Box::new(
        self.configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.into_body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn get_inventory(&self, ) -> Box<dyn Future<Item = ::std::collections::HashMap<String, i32>, Error = Error<serde_json::Value>>> {
        let method = hyper::Method::GET;

        let mut auth_headers = std::collections::HashMap::<hyper::header::HeaderName, String>::new();
        let mut auth_query = std::collections::HashMap::<String, String>::new();
        if let Some(ref apikey) = self.configuration.api_key {
            let key = apikey.key.clone();
            let val = match apikey.prefix {
                Some(ref prefix) => format!("{} {}", prefix, key),
                None => key,
            };
            auth_headers.insert(HeaderName::from_static("api_key"), val);
        };

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            for (ref key, ref val) in &auth_query {
                query.append_pair(key, val);
            }
            query.finish()
        };
        let uri_str = format!("{}/store/inventory?{}", self.configuration.base_path, query_string);

        let mut req = hyper::Request::new(hyper::Body::empty());
        *req.method_mut() = method.clone();

        let uri: hyper::Uri = uri_str.parse().unwrap();
        *req.uri_mut() = uri.clone();

        if let Some(ref user_agent) = self.configuration.user_agent {
            req.headers_mut().insert(hyper::header::CONTENT_TYPE, HeaderValue::from_str(&user_agent).unwrap());
        }


        for (ref key, ref val) in auth_headers {
            req.headers_mut().insert(key, HeaderValue::from_str(&val).unwrap());
        }


        // send request
        Box::new(
        self.configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.into_body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::std::collections::HashMap<String, i32>, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_order_by_id(&self, order_id: i64) -> Box<dyn Future<Item = ::models::Order, Error = Error<serde_json::Value>>> {
        let method = hyper::Method::GET;


        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/store/order/{orderId}?{}", self.configuration.base_path, query_string, orderId=order_id);

        let mut req = hyper::Request::new(hyper::Body::empty());
        *req.method_mut() = method.clone();

        let uri: hyper::Uri = uri_str.parse().unwrap();
        *req.uri_mut() = uri.clone();

        if let Some(ref user_agent) = self.configuration.user_agent {
            req.headers_mut().insert(hyper::header::CONTENT_TYPE, HeaderValue::from_str(&user_agent).unwrap());
        }




        // send request
        Box::new(
        self.configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.into_body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::Order, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn place_order(&self, body: ::models::Order) -> Box<dyn Future<Item = ::models::Order, Error = Error<serde_json::Value>>> {
        let method = hyper::Method::POST;
        let serialized = serde_json::to_string(&body).unwrap();
        let len = serialized.len();


        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/store/order?{}", self.configuration.base_path, query_string);

        let mut req = hyper::Request::new(hyper::Body::from(serialized));
        *req.method_mut() = method.clone();

        let uri: hyper::Uri = uri_str.parse().unwrap();
        *req.uri_mut() = uri.clone();

        if let Some(ref user_agent) = self.configuration.user_agent {
            req.headers_mut().insert(hyper::header::CONTENT_TYPE, HeaderValue::from_str(&user_agent).unwrap());
        }



        req.headers_mut().insert(hyper::header::CONTENT_TYPE, HeaderValue::from_static("application/json"));
        req.headers_mut().insert(hyper::header::CONTENT_LENGTH, HeaderValue::from_str(&len.to_string()).unwrap());

        // send request
        Box::new(
        self.configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.into_body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::Order, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

}
