# Swagger Petstore
# 
# This is a sample server Petstore server.  You can find out more about Swagger at [http://swagger.io](http://swagger.io) or on [irc.freenode.net, #swagger](http://swagger.io/irc/).  For this sample, you can use the api key `special-key` to test the authorization filters.
# 
# OpenAPI spec version: 1.0.0
# Contact: apiteam@swagger.io
# Generated by: https://github.com/swagger-api/swagger-codegen.git

user_api <- R6::R6Class(
  'user_api',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    host = NULL,
    basePath = NULL,
    scheme = NULL,
    url = NULL,
    initialize = function(host, basePath, scheme){
      self$host <- host
      self$basePath <- basePath
      self$scheme <- scheme
      self$url <- sprintf("%s://%s/%s/pet/", scheme, host, basePath)
    },

    create_user = function(body){
      resp <- httr::POST(paste0(self$url), httr::add_headers("User-Agent" = self$userAgent, "content-type" = ""application/xml"")

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
      } else {
        Response$new("Unexpected response status code", resp)
      }

    }
    create_users_with_array_input = function(body){
      resp <- httr::POST(paste0(self$url), httr::add_headers("User-Agent" = self$userAgent, "content-type" = ""application/xml"")

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
      } else {
        Response$new("Unexpected response status code", resp)
      }

    }
    create_users_with_list_input = function(body){
      resp <- httr::POST(paste0(self$url), httr::add_headers("User-Agent" = self$userAgent, "content-type" = ""application/xml"")

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
      } else {
        Response$new("Unexpected response status code", resp)
      }

    }
    delete_user = function(username){
      resp <- httr::DELETE(paste0(self$url, username), httr::add_headers("User-Agent" = self$userAgent, "content-type" = ""application/xml"")

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
      } else {
        Response$new("Unexpected response status code", resp)
      }

    }
    get_user_by_name = function(username){
      resp <- httr::GET(paste0(self$url, username), httr::add_headers("User-Agent" = self$userAgent, "content-type" = ""application/xml"")

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        parsed <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"),
                                 simplifyVector = FALSE)
        result <- User$fromJSON(parsed)
        Response$new(result, resp)
      } else {
        Response$new("Unexpected response status code", resp)
      }

    }
    login_user = function(username, password){
      resp <- httr::GET(paste0(self$url), httr::add_headers("User-Agent" = self$userAgent, "content-type" = ""application/xml"")

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        parsed <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"),
                                 simplifyVector = FALSE)
        result <- string$fromJSON(parsed)
        Response$new(result, resp)
      } else {
        Response$new("Unexpected response status code", resp)
      }

    }
    logout_user = function(){
      resp <- httr::GET(paste0(self$url), httr::add_headers("User-Agent" = self$userAgent, "content-type" = ""application/xml"")

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
      } else {
        Response$new("Unexpected response status code", resp)
      }

    }
    update_user = function(username, body){
      resp <- httr::PUT(paste0(self$url, username), httr::add_headers("User-Agent" = self$userAgent, "content-type" = ""application/xml"")

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
      } else {
        Response$new("Unexpected response status code", resp)
      }

    }
  )
) 
